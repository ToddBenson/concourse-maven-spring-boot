platform: linux

inputs:
  - name: git-repo

run:
  path: git-repo
  args:
  - -exc
  - |
  ls
#  ./mvnw -f git-repo/pom.xml clean install
#  cp ./target/*.war ../release

outputs:
  - name: release

params:
  MAVEN_BASEDIR: git-repo